source 'https://rubygems.org'

gem 'murmurhash3'
gem 'bitarray'

group :doc do
  gem 'redcarpet'       # documentation
  gem 'github-markdown' #  gem install github-markdown -v '0.6.9' --source 'https://rubygems.org/' -- --with-cflags="-Wno-error=implicit-function-declaration"
  gem 'yard'            # documentation
end
